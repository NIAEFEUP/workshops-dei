const submit_btn = document.querySelector("input[type='submit']");

/**
 * Click listener for form's submit button.
 */
submit_btn.addEventListener('click', (event) => {
    console.log('Submitted to SpeakerHub!');
});